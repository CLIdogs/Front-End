DOM:
  文档对象模型，Document Object Model
  可以操作：
    标签（增删改查）、文本、属性、样式、事件

了解：
  DOM的顶级是document，表示当前文档
    PC端：因为document是插入到浏览器（如Chrome）里运行，所以在PC端，document上还有一个window
    移动端：内置解析器的软件的顶级对象不一定是window（浏览器下才是），所以网页必须要在浏览器中打开

  DOM是一个以树状结构存在的内容（DOM树），见DOM.jpg